# HTTP Server in C
Basic implementation for a multithreaded HTTP server in C for unix-like systems

## Build && Run
Use the Makefile to build and run the server

```
make && ./build/server
```

and use the client to connect test the server

```
make && ./build/client
```